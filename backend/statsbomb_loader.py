"""
statsbomb_loader.py — StatsBomb Open Data loader (Phase 2 only).

Package: statsbombpy (pip install statsbombpy)
No API key needed for open data.
Historical data only — not live/current season.

Covers: FA WSL, 2023 Women's World Cup, selected UCL and La Liga seasons.
Use for: xG charts, shot maps, pass networks.

Phase 1: stub only. Do not implement until Phase 2.
"""

# TODO: Implement in Phase 2
# Key functions to build:
#   get_competitions() -> pd.DataFrame
#   get_matches(competition_id, season_id) -> pd.DataFrame
#   get_events(match_id) -> pd.DataFrame   (shot events for xG)
#   get_shot_map(match_id, team_name) -> pd.DataFrame
